# README — 给下次协助本项目的 AI 助手

> 这是上下文交接文档。详细技术文档和全部踩坑史在仓库
> `auto_trade/auto_trade_readme.md`，**动手改任何东西之前先通读它**，
> 尤其是「避坑总结」和「实测修复记录」两节。本文件只是速查摘要。

## 项目是什么

- Pokemon GO 好友自动交易脚本，跑在**按键精灵手机助手**上。
- 仓库 `KilledandKeresetKaddish/anjian_for_pokego_field`：
  - `auto_trade/auto_trade.mq` —— 脚本本体（当前 **v7.10**）
  - `auto_trade/auto_trade_readme.md` —— 内部详细文档（PR#1~#17 避坑总结 + 十五轮实测修复记录）
  - `main reference.md` 已删除，内容并入上面的 readme
- 迭代方式：用户真机实测 → 发日志/截图 → 你对照标定修脚本 → 提 PR。已走过 17 个 PR、15 轮实测。

## Reference 情况

- **按键精灵开发文档：help.anjian.com** —— 写任何命令前先查原生 API，不要凭记忆猜语法。
- **按键精灵论坛：https://bbs.anjian.com** —— 文档查不到的疑难杂症去论坛查。
- 引擎实测事实：颜色一律 **BGR**（引擎解析和 IDE 取色器显示都是 BGR）；
  `FindColor` 区域找色毫秒级，**只返回区域内最上面的匹配点**，未找到返回 -1。

## 用户的两台手机

| 设备 | 角色 |
| --- | --- |
| **iPhone 13 Pro / iOS 16.3**（1170×2532 竖屏） | 开发/标定基准机，历史色值和坐标都在它上面实测 |
| **iPhone 14 Pro Max**（1290×2796 竖屏） | 也必须能跑 |

- 设计要求：**以多机型适配为准**。所有检测区域写屏幕百分比，绝不写死像素。
- 两机宽高比 ≈2.16 几乎一致，纵向百分比可直接迁移；检测区都在屏幕中下部，不碰 14 Pro Max 的灵动岛。
- 上新机型/新界面：先各跑一轮验证，再批量使用。

## 下次不要再犯的错（每条都有真实事故背书）

### 写代码层面
1. **不要用 `GetPixelColor` 逐像素扫区域**——一遍几万次取色等于卡死（v1~v4 的根因）。区域找色只用原生 `FindColor`。
2. **不要做 RGB"兼容"或 R/B 互换检测**——橙色互换后就是蓝色，交易页蓝背景整页误中。只按 BGR 一种口径。
3. **候选点验证失败必须从它下方续搜**——否则一个杂点永远挡住真按钮。
4. **关键按钮（NEXT/CONFIRM/X）不许固定坐标盲点**，必须"等状态 → 点 → 验证切换"；固定坐标只许用于点错也无害的位置。"验证切换"也不能只看"带内同色还没消失"——点击后的失败重点必须同位复核（同一按钮还在原位），否则换页后站进检测带的绿色宝可梦模型会顶替 NEXT 触发盲目重点（v7.7 实测翻车）。**结构验证必须从首检就全量生效**：只加在重试分支上 = 把假候选放进流程再亡羊补牢（v7.8 实测翻车）。**且候选验证 ≠ 页面验证**：首检可能跑在还没切走的上一页（白底选择列表）上，候选"长得全对"（绿长条 + 白色像素），错的是页面本身——列表背景白把白字验证喂饱了；点击前必须自证页面身份（NEXT 确认页蓝底 vs 列表白底，左翼找白一票拦截，v7.9 实测翻车后修复）。
5. **单点/单段小窗的颜色验证必被骗**（青色文字、功能图标、页签下划线、抗锯齿灰——文字灰边和糖果球图标灰都中过招、白底好友页、绿色宝可梦模型整块实心绿全都骗过）——必须组合结构验证：实心连续多窗 + 纵向厚度 + 总长 + 排长条 + 位置约束 + 贴左/白字（交易页按钮从屏幕左缘伸出且印纯白文字，模型两样都没有）。

### 改检测逻辑层面
6. **不要凭猜改色盒**——v7.4 (PR#10) 没有任何实测色值就换 spec，合并 12 分钟被整体回滚。修复必须有用户实测的色值/坐标支撑。
7. **不要为修 A 处改共用色盒殃及 B 处**——正确做法是新增专用 spec（v7.6 加 `XSolidSpec` 而一字不动 `CloseSpec`，因为后者还在好友页锚点并集里用）。
8. **色盒要排中性灰**：文字抗锯齿灰（R≈G≈B，132~155）是惯犯。关键判定用「G≥178 且 R≤142」这类数学上排除 R=G=B 的写法。
9. **不要破坏安全不变量**，任何改动前论证它们仍成立：
   - 详情页标记「在好友页恒为 False」；
   - 好友页锚点「在详情页恒不成立」（绿长钮硬安全闸保证）；
   - 等对方确认期间绝不重复点 CONFIRM 位置（会点到 CANCEL，交易作废）；
   - 弹窗上绝不点主按钮（那是 YES = 真进化/强化，不可逆），只点下方 NO；
   - 轮前全程绝不补点 X（点掉好友页自己的 X 翻过三次车）。
10. **风险分级响应**：检出绿色长按钮 = 可能误进化/耗星尘 → 硬停交人工；无副作用的疑似情况 → 告警放行交超时兜底。一刀切两头（只告警继续 / 确认即硬停）都翻过车。
11. **一切重试/补点都加保险丝**（识别 X ≤3 次、兜底 ≤4 档、弹窗 ≤4 次这类），用完打诊断安全退出，绝不无限重试。
12. **UI 随时会改版**（图标改彩色插画打穿过锚点、好友页出过白底样式）——检测逻辑必须配可校准的分解诊断日志，失败时一行日志能看出哪个窗、哪个色谱没命中。

### 流程/文档层面
13. **每次改脚本必须 bump 版本号**（脚本头注释 + 启动日志第一行），并提醒用户测试前核对——误测旧版出过事故。
14. **PR 描述和文档写准史实**：分支带了什么 commit 就写什么（PR#9 实际带 v7.2+v7.3 但描述只写 v7.2；"v7.4 未合并"其实是合并后被回滚）。
15. **先要证据再动手**：出问题先要日志（每步都打"等什么、等了多久"）和截图，对照标定后再修；排障路径 = 日志定位卡点 → 取色器实测（BGR）→ 调对应 `*Spec` 或区域百分比。
16. **方向错了整体 revert 再重做**（#11 回滚 #10、#12 重做），不要在错误方案上打补丁。

## 当前状态（截至 2026-06-10）

- main = **v7.9**（PR#16 已合并：v7.8 白字验证提前到 NEXT 首检 + v7.9 "左翼蓝底"页面身份验证，修"首检跑在还没切走的白底选择列表页上"）。
- 本分支 = **v7.10**（PR#17，开放）：修第十五轮"**13 Pro 收到 Scatterbug 后收尾零动作**——动画放完停在详情页没人点 X，150 秒超时退出；同笔交易 14 Pro Max 收 Beldum 正常"。截图对照标定的事故链：Scatterbug 详情页资源栏只有一行三格，POWER UP 行被抬进锚点检测带 73.5%~78.8%——LT 窗吃到按钮绿身、BT 窗吃到强化成本行**糖果球图标的抗锯齿灰**（宽谱 `CloseSpec` 的 132~155 中性灰漏洞，与 v7.6 mega 灰字同族第三次发作），**好友页锚点在详情页上误成立、把页面标记一票否决**；Beldum 页多一行 mega 能量、按钮区被压低 ~5%，成本行在带外所以没事。修法：① 锚点 LT/BT 图标窗并集的青色腿 `CloseSpec`→`XSolidSpec`（饱和青，灰在数学上进不了盒；新函数 `IconSolidSpecHitIn`，间隙净空窗保持宽谱不动）；② 完成页等待超 ~36 实秒及 150 秒超时退出前补打 `标记诊断` + `锚点分解`（此前该超时路径是全脚本唯一无探针诊断的失败出口），`锚点分解` 加宽青对照列。**毫米级标定待用户日志复核**：按钮右缘是否过 46% 间隙窗、绿长钮闸为何没拦住（怀疑渐变左半 R<60 导致候选越过 `DetLeftMaxX` 25%，绿探针可能一直靠白卡列默默兜底）——下轮卡死日志里的三行诊断可直接定位。
- 已知边界：特殊交易（幸运/异色/未登录图鉴）弹窗不处理；单钮提示弹窗（OK）不会被自动关，需手动；好友不在附近时 BATTLE 图标变灰，每轮多等 10 秒走兜底；NEXT 左翼蓝底验证假设确认页是蓝底（改版白底会误拦，超时安全退出可由日志定位）；v7.10 起锚点图标窗只认饱和色谱，将来若图标改版成弱青/灰青样式会漏认（良性降级：多等 10 秒 + 告警放行，`锚点分解` 现"宽青1 饱青0"指纹）。
